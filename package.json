{
  "name": "mai-bank",
  "version": "1.0.0",
  "description": "In-memory account ledger",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "jest",
    "test:supplementary": "jest -c jest.supplementary.config.js",
    "benchmark": "ts-node supplementary/benchmark.ts",
    "start": "node dist/runner.js",
    "runner": "ts-node src/runner.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.43",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.12",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
